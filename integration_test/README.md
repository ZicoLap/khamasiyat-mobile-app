# Integration tests

Integration tests will be added when product flows exist (F1+).

Run later with:

```bash
flutter test integration_test
```

F0 keeps this folder as a placeholder so the `integration_test` package is ready.
